# {props.title}

<p>{props.description}</p>

<p>
  <u>
    Tags: <span>{props.tags.map((t) => `#${t}`).join(', ')}</span>
  </u>
</p>

:::note Content wanted

This topic page is scaffolding — the summary above is pulled from
`docs/topics/_configs.ts`. Add the long-form sections below using the starters
in [Templates](/docs/templates/).

:::
