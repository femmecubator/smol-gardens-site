import ContentWithMedia from '@site/src/components/ContentWithMedia'
import topicImage from './assets/model-right-sizing.jpg'

<ContentWithMedia
  src={topicImage}
  alt="Placeholder image: an Akita dog standing on grass beside a pond"
>

## {props.title}

<p>{props.description}</p>

<p>
  <u>
    Tags: <span>{props.tags.map((t) => `#${t}`).join(', ')}</span>
  </u>
</p>

</ContentWithMedia>

:::note Content wanted

This topic page is scaffolding — the summary above is pulled from
`docs/topics/_configs.ts`. Add the long-form sections below — see
[Topic page](/docs/templates/topic-page) for the page structure.

:::
